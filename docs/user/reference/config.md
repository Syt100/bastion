# Configuration and environment reference

This page is generated from Bastion's actual CLI definitions plus a small curated set of env-only runtime knobs.

To refresh it after changing configuration flags or env handling:

```bash
cargo run -p bastion --bin docgen -- --write
```

<!-- @include: ./config.generated.md -->
