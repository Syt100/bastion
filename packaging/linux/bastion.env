# Bastion systemd environment overrides.
#
# This file is read by `bastion.service` and provides environment variables for the Hub.
# After editing, apply changes with:
#   sudo systemctl restart bastion

# Data directory (SQLite + encrypted secrets).
BASTION_DATA_DIR=/var/lib/bastion

# Bind host/port (defaults: 127.0.0.1:9876).
#BASTION_HOST=127.0.0.1
#BASTION_PORT=9876

# Explicitly allow insecure HTTP/WS mode (LAN/dev only).
#BASTION_INSECURE_HTTP=1

# Logging (defaults to journald).
#BASTION_LOG=info,tower_http=warn
#BASTION_LOG_FILE=/var/log/bastion/bastion.log
#BASTION_LOG_ROTATION=daily
#BASTION_LOG_KEEP_FILES=30
