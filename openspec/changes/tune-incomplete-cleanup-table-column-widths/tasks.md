## 1. Spec
- [x] 1.1 Add `web-ui` spec delta for desktop column width balance
- [x] 1.2 Run `openspec validate tune-incomplete-cleanup-table-column-widths --strict`

## 2. UI
- [x] 2.1 Reduce “目标/状态” column widths on desktop
- [x] 2.2 Increase “最近错误” column width on desktop (keep single-line truncation + full-text access)

## 3. Verification
- [x] 3.1 Run `cd ui && npm test` and `npm run type-check`
