## 1. Implementation

- [x] 1.1 Add/refresh the Web UI style guide docs (EN + ZH).
- [x] 1.2 Link the style guide from the docs index pages and sidebar navigation.
- [x] 1.3 Update UI templates so any `n-card` with `class="app-card"` uses `:bordered="false"` (including mobile drawer cards where applicable).
- [x] 1.4 Add a regression test that fails when `app-card` is used without `:bordered="false"`.
- [x] 1.5 Run UI tests and `scripts/ci.sh`.
