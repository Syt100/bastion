# update-webdav-direct-upload-safety-and-alert-digest

Make WebDAV raw-tree direct upload safer (concurrency + rate limiting), make direct upload explicitly configurable per job/pipeline, and de-noise/aggregate run warnings so the runs list shows high-signal summaries while run detail keeps full evidence.

